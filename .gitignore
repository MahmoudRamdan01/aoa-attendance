.vercel

# Don't ship source maps to production (they re-expose full source and bloat load).
*.map

# v1 build artifacts — React source lives in v1-src/, built output is committed under v1/
node_modules/
v1-src/dist/
v2-src/dist/
v2-src/node_modules/
.claude/

# Local-only offline demo (mock data + throwaway build) — never shipped.
v1-src/demo-dist/
v1-src/src/lib/demoClient.js
v1-src/src/lib/demoData.js
